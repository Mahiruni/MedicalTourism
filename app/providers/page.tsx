import Link from 'next/link'
import { VerificationGraphic } from '@/components/Graphics'
import { MedicalIcon } from '@/components/MedicalIcon'
export const metadata = { title: 'Hospitals & Doctors', description: 'How TenaBridge evaluates hospital and doctor fit before medical travel.' }
const checks=[['License','Current professional or facility status.'],['Specialty fit','Relevant clinical capability for the case.'],['Affiliation','Where the clinician actually treats.'],['Quote scope','What is and is not included.'],['Timing','Review, procedure and expected stay.'],['Follow-up','What happens after discharge.']]
export default function Providers(){return <main id="main-content"><section className="pageHero"><div className="shell pageHeroGrid"><div><span className="eyebrow">Hospitals & doctors</span><h1>Verification before recommendation.</h1><p>We do not publish invented partners, badges or outcomes.</p><Link className="button buttonBlue" href="/contact">Request provider options</Link></div><div className="graphicPanel"><VerificationGraphic/></div></div></section><section className="section"><div className="shell providerCheckGrid">{checks.map(([title,copy],i)=><article key={title}><span>{String(i+1).padStart(2,'0')}</span><MedicalIcon name={i<2?'doctor':i===2?'hospital':'shield'}/><h3>{title}</h3><p>{copy}</p></article>)}</div></section><section className="section paleSection"><div className="shell twoCol"><div><span className="eyebrow">Profiles</span><h2>Real names only when verified.</h2></div><div className="shortList"><p><b>Hospital profile</b><span>Services, location, accreditations and international-patient support.</span></p><p><b>Doctor profile</b><span>Specialty, credentials, languages and treating hospital.</span></p><p><b>Patient story</b><span>Published only with consent and accurate context.</span></p></div></div></section></main>}
