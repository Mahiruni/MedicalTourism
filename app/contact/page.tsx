'use client';
import { FormEvent,useState } from 'react';
export default function Contact(){const[sent,setSent]=useState(false);function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setSent(true)}return <section className="contactPage"><div className="shell contactGrid"><div><span className="eyebrow">Free consultation</span><h1>Start with a few details.</h1><p>Do not upload sensitive medical records here. A secure document pathway should be connected before production.</p></div><form onSubmit={submit} className="contactForm"><label>Name<input required name="name"/></label><label>Email<input required type="email" name="email"/></label><label>Care direction<select name="direction"><option>Care in Ethiopia</option><option>Care abroad</option></select></label><label>Treatment need<textarea required name="need" rows={4}/></label><button className="button">Send inquiry</button>{sent&&<p className="success">Thanks — connect this form to your secure CRM before launch.</p>}</form></div></section>}
